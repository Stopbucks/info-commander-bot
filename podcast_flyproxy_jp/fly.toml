# fly.toml app configuration file generated for podcast-flyproxy-jp
#
# ✅ 開啟 TCP 直通模式，移除 http_service 懶人包
#

app = 'podcast-flyproxy-jp'
primary_region = 'nrt'

[build]

# 👇 這是手動設定區塊，取代原本的 [http_service]
[[services]]
  protocol = "tcp"
  internal_port = 8080
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

  # 關鍵：Port 443 只做 TLS 解密，不做 HTTP 檢查
  [[services.ports]]
    port = 443
    handlers = ["tls"]

  # Port 80 強制轉址
  [[services.ports]]
    port = 80
    handlers = ["http"]
    force_https = true

[[vm]]
  memory = '1gb'
  cpus = 1
  memory_mb = 1024