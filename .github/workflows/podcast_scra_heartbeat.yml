##########################################################################
# 🏹 此程式檔：podcast_scra_heartbeat.yml
##########################################################################
# 1. 任務目標：負責叫醒vercel 進行巡邏
# 2. 觸發機制：00:00, 06:00, 12:00, 18:00 (對應 TW 08:00, 14:00, 20:00, 02:00)
##########################################################################

name: S-Plan External Trigger

on:
  schedule:
    # 🚀 UTC 00:00, 06:00, 12:00, 18:00 (對應 TW 08:00, 14:00, 20:00, 02:00)
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch: # 允許指揮官手動測試

jobs:
  ping_scout:
    runs-on: ubuntu-latest
    steps:
      - name: 📡 發動遠程喚醒指令
        run: |
          # 發送帶有 X-API-KEY 標頭的請求，確保通過 Vercel 軟體防火牆。
          curl -s -o /dev/null -w "Status: %{http_code}" \
            -H "X-API-KEY: ${{ secrets.VERCEL_AUTH_TOKEN }}" \
            "${{ secrets.VERCEL_SCOUT_URL }}"

 