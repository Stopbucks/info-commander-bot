##########################################################################
# 🧪 S-Plan 特種物流演習 (Chunked Relay Transport)
# 任務：測試 WebScraping.ai 分段搬運 1MB 片段與 FFmpeg 縫合技術
##########################################################################
name: 🧪 S-Plan Relay Test

on:
  workflow_dispatch: # 僅限手動觸發，節省點數

jobs:
  transport_test:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    env:
      WEBSCRAP_API_KEY: ${{ secrets.WEBSCRAP_API_KEY }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
      R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      PYTHONUNBUFFERED: 1

    steps:
    - name: 📦 檢出程式碼
      uses: actions/checkout@v4

    - name: 🐍 設定 Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: ⚙️ 安裝環境依賴與 FFmpeg
      # 一行註解：安裝搬運所需的二進位處理工具及 FFmpeg。
      run: |
        sudo apt-get update && sudo apt-get install -y ffmpeg
        python -m pip install --upgrade pip
        pip install requests boto3 supabase python-dotenv

    - name: 🎯 執行分段搬運測試
      # 一行註解：執行核心搬運腳本，觀察擬態緩衝與分段縫合結果。
      run: python src/test_chunked_transport.py