##########################################################################
# 📡 .github\workflows\tg_debug_test.yml (Telegram 通訊官診斷測試)
##########################################################################
name: 📡 TG Debug Test

on:
  workflow_dispatch: # 支援手動觸發

jobs:
  comms_check:
    runs-on: ubuntu-latest
    steps:
#--- 請確保這段程式碼的縮進與 image_77b1bd 相比更為整齊 ---#
      - name: 📦 檢出程式碼
        uses: actions/checkout@v4

      - name: 🐍 設定 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: ⚙️ 安裝必要依賴
        run: pip install requests

      - name: 📡 發起連線測試
        env:
          # 一行註解：確保金鑰名稱與 Secrets 列表 image_78b3eb 完全一致。
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python src/test_tg_comms.py