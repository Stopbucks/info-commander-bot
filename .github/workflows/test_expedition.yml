
##########################################################################
# 🧪 S-Plan 偵察兵火力測試 (Expedition Module - 三維度擴充版)
# 任務：測試各供應商對 Podbay, Listen Notes 或 官方伺服器(OFFICIAL) 的穿透力
##########################################################################
name: 🧪 S-Plan Scanner Test

on:
  workflow_dispatch:
    inputs:
      test_provider:
        description: '🧪 選擇欲測試的偵察部隊 (供應商)'
        required: true
        default: 'ZENROWS'
        type: choice
        options: 
          - ZENROWS
          - WEBSCRAPING
          - SCRAPEDO
          - HASDATA
      # 一行註解：新增 OFFICIAL 選項，用於測試官方伺服器（如 Megaphone, Omny）的網址活性化。
      test_site:
        description: '🎯 選擇偵察目標站點'
        required: true
        default: 'PODBAY'
        type: choice
        options:
          - PODBAY
          - LISTEN_NOTES
          - OFFICIAL

jobs:
  scanner_expedition:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
      R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
      
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      
      SCRAP_API_KEY: ${{ secrets.SCRAP_API_KEY }}
      ZENROWS_API_KEY: ${{ secrets.ZENROWS_API_KEY }}
      WEBSCRAP_API_KEY: ${{ secrets.WEBSCRAP_API_KEY }}
      SCRAPEDO_API_KEY: ${{ secrets.SCRAPEDO_API_KEY }}
      HASDATA_API_KEY: ${{ secrets.HASDATA_API_KEY }}

      RENDER_WEBHOOK_URL: ${{ secrets.RENDER_WEBHOOK_URL }}
      CRON_SECRET: ${{ secrets.CRON_SECRET }}
      
      # 🎯 映射選單：將 GitHub UI 的選擇精準對位至 Python 環境變數。
      TEST_PROVIDER_MODE: ${{ github.event.inputs.test_provider }}
      TEST_SITE_TARGET: ${{ github.event.inputs.test_site }}
      PYTHONUNBUFFERED: 1

    steps:
    - name: 📦 檢出程式碼
      uses: actions/checkout@v4

    - name: 🐍 設定 Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: ⚙️ 安裝環境依賴
      # 一行註解：安裝基礎解析工具，此測試腳本不需 ffmpeg 或 AI 模組以節省啟動時間。
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4 supabase python-dotenv boto3

    - name: 🎯 發動偵察演習 (Expedition)
      # 一行註解：啟動測試主程序，根據傳入的站點模式執行不同偵察邏輯。
      run: |
        echo "🚀 [發動] 供應商：$TEST_PROVIDER_MODE | 目標站點：$TEST_SITE_TARGET"
        python src/test_scanner_expedition.py