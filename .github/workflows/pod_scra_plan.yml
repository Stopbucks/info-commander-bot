##########################################################################
# 🏃 S-Plan 4.0 全鏈條接力 - 戰略資源自動重設版
# scraper & zenrows 固定每月1日喚醒進行恢復點數，其餘周一~五 03:00 執行任務
##########################################################################

name: 🏃 S-Plan podcast

on:
  schedule:
    # 戰術排程一：週一至週五凌晨 03:00 (UTC 19:00) 執行例行任務
    - cron: '0 19 * * 1-5'
    # 戰術排程二：每月 1 日凌晨 03:00 (UTC 19:00) 強制喚醒，確保點數重置 (不論是否為週末)
    - cron: '0 19 1 * *'
  
  workflow_dispatch:
    inputs:
      force_mode:
        description: '🚀 戰略模式切換 (AUTO / MODE_1 / MODE_2)'
        required: true
        default: 'AUTO'
        type: choice
        options:
        - AUTO
        - MODE_1
        - MODE_2

jobs:
  relay_mission:
    runs-on: ubuntu-latest
    # 一行註解：將超時延長至 180 分鐘，確保包含 Jitter 與任務間隔的高壓環境能安全完成。
    timeout-minutes: 180 
    
    steps:
    - name: 📦 檢出程式碼
      uses: actions/checkout@v4

    - name: 🐍 設定 Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: ⚙️ 安裝必要依賴
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4 supabase python-dotenv boto3 google-generativeai groq
        
    - name: 🎯 執行解碼任務 (Officer)
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        SCRAP_API_KEY: ${{ secrets.SCRAP_API_KEY }}
        ZENROWS_API_KEY: ${{ secrets.ZENROWS_API_KEY }}
        # 一行註解：處理手動觸發(inputs)與自動排程(schedule)的變數對位。
        STRATEGY_MODE: ${{ github.event.inputs.force_mode || 'AUTO' }}
        PYTHONUNBUFFERED: 1
      run: |
        echo "📡 [啟動] 解碼官執行任務，當前戰略模式: $STRATEGY_MODE"
        python src/pod_scra_officer.py
    #--- 執行運輸兵任務之前先等待 ---#
    - name: 🕒 戰術觀察期 (等待解碼官產出)
      run: sleep 560  # 強制等待約 8 分鐘，確保解碼官完成寫入

    #--- 「🚚 執行運輸與 AI 摘要任務」區塊 ---#
    - name: 🚚 執行運輸與 AI 摘要任務
      timeout-minutes: 60
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
        R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        PYTHONUNBUFFERED: 1
      run: |
        python src/pod_scra_transport.py
# -----(定位線)以上為補全內容----